import React from 'react';

// Define a Header component
const Header = () => {
    // The component returns a div with a h1 heading
    return (
        <div className="header-container">
            <h1 className="header-text">Vehicle Vin Finder</h1>
        </div>
    );
};

// Export the Header component for use in other files
export default Header;
